{
  "name": "@evolvr/cli",
  "version": "0.1.0",
  "description": "evolvr CLI — manage the task journal and lessons from the terminal",
  "type": "module",
  "bin": {
    "evolvr": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@evolvr/core": "workspace:*",
    "commander": "^12.1.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0"
  }
}
